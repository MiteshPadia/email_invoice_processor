All the tasks are divided as per file names 
Structure is given in config files 
The project is a email processor which looks for files with keywords given in arguments,
stores it in s3 and uses AWS extract to convert it into a json file 
